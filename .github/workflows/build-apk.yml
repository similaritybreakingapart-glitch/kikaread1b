name: Build Kika Mobile APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: kika_mobile/package-lock.json

      - name: Install dependencies
        working-directory: ./kika_mobile
        run: npm ci

      - name: Build Web Assets
        working-directory: ./kika_mobile
        run: npm run build

      - name: Sync Capacitor Android
        working-directory: ./kika_mobile
        run: npx cap sync android

      - name: Set up Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Make Gradlew Executable
        working-directory: ./kika_mobile/android
        run: chmod +x gradlew

      - name: Build Android Debug APK
        working-directory: ./kika_mobile/android
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: kika-reader-debug-apk
          path: kika_mobile/android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
